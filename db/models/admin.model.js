const db = require("../db.js")

// all methods for interacting with the admin model
// this should be pretty restrictive as there should be few cases where
// information concerning an admin will change
const Admin = function(){
    // construct admin
}
Admin.login = function(adminName, callback){

}


module.exports = Admin